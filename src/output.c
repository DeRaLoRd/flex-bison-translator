/* Translated through PTSD - Pascal To "Si" translaDor
 * By Vasiliy Tarasevich 2024
 * Original programme name - Source
 */

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <stddef.h>

// Global constants
const int bidorasy = 4;

// Global variables
int x = 3;
double y = 2.3;
short z = 52;
int result;

void foo(int a, double b)
{
	y = x + 3;
}
void knee_surgery(double knee)
{
	result = knee + surgery;
}

int main()
{
	if (z < knee)
	{
		z = 52;
	}
	for (int ptsd_iterator0 = 0; ptsd_iterator0 < 15; ptsd_iterator0++)
	{
		result = result + z * x - 4;
	}
	while (!y > 4)
	{
		y = y - 1;
	}
	do
	{
		printf("%lf\n", y);
		y = y + 1;
	}
	while (y > 13);
	for (int i = 10; i > 0; i--)
	{
		x = x + 1;
	}
	return 0;
}